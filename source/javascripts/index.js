document.addEventListener("DOMContentLoaded", function () {
  try {
    handleReveal();
    handleCart();
    // handleInputs();
    handleMobileMenu();
    // handleScrollDir();
  } catch (error) {
    console.log(error);
  }
});
