document.addEventListener("DOMContentLoaded", function () {
  try {
    handleReveal();
    handleCart();
    // handleInputs();
    handleMobileMenu();
    handleLinkClick();
    // handleScrollDir();
  } catch (error) {
    console.log(error);
  }
});
