document.addEventListener("DOMContentLoaded", function () {
  try {
    handleReveal();
    handleCart();
    handleMobileMenu();
    handleLinkClick();
    handleDescription();
    handleMagnet();
    handleInputs();
    handleZoomImage();
  } catch (error) {
    console.log(error);
  }
});
