document.addEventListener("DOMContentLoaded", function () {
  try {
    // handleLazyLoad();
    handleReveal();
    handleCart();
    handleMobileMenu();
    handleModal();
  } catch (error) {
    console.log(error);
  }
});
